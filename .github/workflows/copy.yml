name: Sync Repository

on:
  push:
    branches:
      - main  # 触发条件可以根据你的需求进行修改

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v2  # 检出当前仓库
        with:
          repository: ${{ github.repository }}  # 使用当前仓库

      - name: Checkout destination repository
        uses: actions/checkout@v2  # 检出目标仓库
        with:
          repository: itangqiao/pic  # 目标仓库的用户名和仓库名
          ref: main  # 手动指定目标仓库的默认分支为 main
      
      - name: Sync .webp files to destination repository
        run: |
          pwd
          echo "---------- Current Repository ----------"
          cd ./
          ls
          echo "---------- Destination Repository ----------"
          
      # - name: Sync changes
      #   run: |
      #     git config --local user.email "173467694@qq.com"
      #     git config --local user.name "itangqiao"
          
      #     # 确认一下工作目录和文件路径
      #     pwd
      #     ls
          
      #     # 添加并提交 WebP 图片文件
      #     git add .
      #     git commit -m "Sync changes"
          
      #     # 推送到目标仓库
      #     git push origin main
